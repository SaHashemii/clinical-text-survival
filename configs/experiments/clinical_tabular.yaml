experiment:
  name: clinical_unimodal_tabular
  task: unimodal_survival
  model_type: clinical_unimodal
  output_dir: outputs/clinical_unimodal_tabular

data:
  label_name: without_urolife
  clinical_source: tabular

cv:
  n_splits: 5
  val_size: 0.20
  seed: 42

model:
  hidden_dims: [256, 128]
  projection_dim: 128
  dropout: 0.20
  activation: selu

training:
  epochs: 300
  patience: 40
  batch_size: 64
  min_events_per_batch: 3
  training_style: full_batch
  lr: 0.0002
  weight_decay: 0.00001
  grad_clip: 5.0
  device: cuda

